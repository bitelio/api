language: python
python:
  - 3.5
services:
  - mongodb
install:
  - pip install -r requirements.txt
before_script:
  - python seed.py
script:
  - nosetests
addons:
  apt:
    sources:
      - mongodb-3.2-precise
    packages:
      - mongodb-org-server
